[quiz]
# Maximum number of questions to load per subject.
# If the subject has fewer questions than this limit, all questions are loaded.
max_questions = 10

[database]
host = localhost
port = 5432
database = quiz_db
user = quiz_user
password = quiz_password
