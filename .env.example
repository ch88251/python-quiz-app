# PostgreSQL Database Configuration
# Copy this file to .env and modify as needed

# Database connection settings
DB_HOST=localhost
DB_PORT=5432
DB_NAME=quiz_db
DB_USER=quiz_user
DB_PASSWORD=quiz_password

# Docker volume path (modify if needed)
POSTGRES_DATA_PATH=/home/chayes/quiz_data
